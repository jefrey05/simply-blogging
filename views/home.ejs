<%- include('partials/header') -%>

<nav class="mt-5" >
    <div class="d-flex justify-content-around">
        <a href="/home" class=link-primary>Feed</a>
        <a href="/blog/createBlog" class="link-info">Create Blog</a>
        <a href="/blog/popularBlogs" class="link-primary">Popular Blogssss</a>
        <!-- will need to obtain user object to verify if user logged in order to display my profile option -->
        <a href="/profile" class="link-primary">My Profile</a>
        <a href="/logout" class="link-primary">Logout</a>
    </div>
</nav>
<!-- <div class="container">
    <div class="row justify-content-center mt-5">
      <ul class="row list-unstyled">
        <% for(var i=0; i<blogs.length; i++) {%>
          <li class="col-6 justify-content-between mt-5">
            <h5><%=blogs[i].title%></h5>
            <a href="/blog/<%= blogs[i]._id%>">
              <img class="img-fluid" src="<%= blogs[i].image%>">
            </a>
            <h5><%=blogs[i].caption%></h5>
          </li>
        <% } %>
        </ul>
    </div>
  </div>  -->
  <div class="container">
  <div class="row row-cols-1 row-cols-md-4 g-3 feeder">
    <% for(var i=0; i<blogs.length; i++) {%>
    <div class="col">
      <div class="card fit">
        <!-- <img src="<%= blogs[i].image%>" class="card-img-top" alt="..."> -->
        <a href="/blog/<%= blogs[i]._id%>">
          <img class="img-fluid half" src="<%= blogs[i].image%>">
        </a>
        <div class="card-body">
          <h5 class="card-title"><%=blogs[i].title%></h5>
          <p class="card-text"><%=blogs[i].caption%></p>
          <div class="like-favorite">
            <form
          class="col-1"
          action="/blog/likeBlog/<%= blogs[i]._id %>?_method=PUT"
          method="POST"
        >
          <button class="btn btn-primary fa fa-heart" type="submit"></button>
            </form>

            <form
          action="/blog/favoriteBlogHome/<%= blogs[i]._id %>"
          method="POST"
          class="col-4"
        >
          <button class="btn btn-primary fa fa-star" type="submit"></button>
        </form>
          </div>
        </div>
      </div>
    </div>
    <% } %>
  </div>
  </div>
<%- include('partials/footer') -%>